import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:firebase_messaging_app/app/modules/app_pushs/app_pushs_page.dart';

void main() {
  testWidgets('AppPushsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AppPushsPage(title: 'AppPushs')));
    //  final titleFinder = find.text('AppPushs');
    //  expect(titleFinder, findsOneWidget);
  });
}
